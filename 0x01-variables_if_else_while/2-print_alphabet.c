#include <stdlib.h>
#include <time.h>

/*
 * main - Prints out the alphabet in lowercase between a-z
 * return: 0 (success)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z"; letter ++)
		putchar(letter);
	putchat('\n');

	return (0);
}
